package com.cydeo.week3;

public class CssLocators {

   /* public static void main(String[] args) {
        WebDriverManager.chromedriver().setup();
        WebDriver driver=new ChromeDriver();
        driver.manage().window().maximize();

        //syntax of css
        //tagname[attributeName='Value']

       // driver.get("https://practice.cydeo.com/inputs");
       // WebElement flas=driver.findElement(By.cssSelector())
        driver.get("https://practice.cydeo.com/");
        //WebElement dropDown= driver.findElement(By.cssSelector("select#state>option:nth-of-type(10)"));
        WebElement dropDown= driver.findElement(By.cssSelector("//h2[text()='Forgot Password']"));
        //2. providing syntax, only used for id or classname
        //tagName.classValue or tagName#id
        //div#  or div.container
        //if we use class value, have to replace space with"."
        //div.example>h2, first locate parent and then child
        //div.example>form,
        //in css only locate from parent to child, not child to parent

        //select#state>option:nth-of-type(6)

        // //div[contains(@id,'drop')]
        // //div[contains(@id,'foot')]

        // //h2[text()='Forgot Password']
        // //h2[.='Forgot Password']
        // *[@id='form_submit']
        // in xpath child to parent: /..
        // //button[@id='form_submit']/..
        // //option[.='Colorado']/preceding-sibling::option
        //  //option[.='Colorado']/following-sibling::option
        // //option[.='Colorado']/following-sibling::option[40]
        // (//option[.='Colorado']/following-sibling::option)[40]

    */
}


